#include <stdio.h>
#include <math.h>

int main( void )
{
	double x;

	x = sin(1);
	printf( "sin(1) : %g \n", x );

	x = cos(1);
	printf( "tan(1) : %g \n", x );

	x = tan(1);
	printf( "tan(1) : %g \n", x );

}
