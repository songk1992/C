#include <stdio.h> 

int main(void) 
{ 
	char ch;
	unsigned char j;
	char k;

	ch = 200;
	j = 200;
	k = 'a';

	printf( "문자형 변수 i의 값은 %d \n", ch );
	printf( "문자형 변수 j의 값은 %u \n", j );
	printf( "문자형 변수 k의 값은 %d \n", k );
} 
